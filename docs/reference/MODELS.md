# 🤖 Описание моделей и анонимизаторов

## 📋 Обзор анонимизаторов (актуальные результаты)

Система **Anonymizer** включает 6 различных анонимизаторов, каждый из которых имеет свои преимущества и области применения.

| Анонимизатор | Технология | F1 Micro | F1 Macro | Precision | Recall | Скорость | Рекомендуемое использование |
|-------------|------------|----------|-----------|-----------|--------|----------|---------------------------|
| **RegExp Baseline** | Регулярные выражения | **0.499** | **0.275** | **0.654** | **0.403** | Очень быстрая | 🏆 **Лучший общий + телефоны** |
| **Natasha Enhanced** | Морфологический анализ | 0.237 | 0.060 | 0.351 | 0.179 | Быстрая | Для русских имен/организаций |
| **Hybrid Advanced** | Комбинированный | 0.236 | 0.066 | 0.377 | 0.172 | Средняя | Для сложных случаев |
| **SpaCy Extended** | NLP пайплайн | 0.225 | **0.074** | 0.307 | 0.177 | Быстрая | Универсальный |
| **DeepPavlov NER** | Transformer (BERT) | 0.205 | 0.056 | 0.273 | 0.165 | Медленная | Экспериментальный |
| **Transformers BERT** | BERT + fine-tuning | 0.190 | 0.042 | 0.246 | 0.156 | Медленная | В разработке |

### 📊 Интерпретация метрик
- **F1 Micro** - взвешенная метрика с учетом количества сущностей каждого типа
- **F1 Macro** - усредненная метрика по всем типам (показывает работу с редкими типами)
- **Критически низкий F1 Macro** у всех анонимизаторов указывает на специализацию только на частых типах

**⚠️ Важное замечание:** Текущие результаты показывают, что все анонимизаторы требуют серьезной доработки для использования в продакшене. Целевой F1 Micro должен быть выше 0.8, F1 Macro - выше 0.6.

## 🔧 Детальное описание анонимизаторов

### 1. 🏆 RegExp Baseline Anonymizer

**Файл:** `anonymizers/regexp_baseline/regexp_anonymizer.py`

**Описание:** Анонимизатор на основе регулярных выражений, показывающий лучшие общие результаты.

**Технология:**
- Оптимизированные регулярные выражения для разных типов ПД
- Быстрая обработка без загрузки моделей
- Специализированные паттерны для телефонов, email, банковских карт

**Сильные стороны (по актуальным данным):**
- **PHONE**: F1=0.937 (превосходно) - 90 TP, 5 FP, 7 FN
- **EMAIL**: F1=0.556 (средне) - 35 TP, 29 FP, 27 FN  
- **BANK_CARD**: F1=0.913 - отличная работа с картами
- **SNILS**: F1=0.240 - базовая поддержка
- **Общие метрики**: F1 Micro=0.499, F1 Macro=0.275, Precision=0.654

**Слабые стороны:**
- **PERSON**: F1=0.533 (средне) - пропускает сложные имена
- **LOCATION**: F1=0.000 - не работает с адресами
- **ORGANIZATION**: F1=0.000 - не находит организации
- **Низкий F1 Macro**: плохо работает с редкими типами сущностей

**Пример использования:**
```python
from anonymizers.regexp_baseline.regexp_anonymizer import RegExpBaselineAnonymizer

anonymizer = RegExpBaselineAnonymizer()
text = "Мой телефон +7-926-123-45-67, email test@example.com"
result = anonymizer.anonymize(text)
print(result)
# "Мой телефон <PHONE>, email <EMAIL>"
```

### 2. 🌟 Natasha Enhanced Anonymizer

**Файл:** `anonymizers/natasha_enhanced/natasha_anonymizer.py`

**Описание:** Специализирован для русскоязычных имен и организаций.

**Сильные стороны (по актуальным данным):**
- **PERSON**: F1=0.862 (отлично) - 78 TP, 14 FP, 11 FN
- **ORGANIZATION**: F1=0.291 (лучший среди всех) - 15 TP, 62 FP, 11 FN
- **LOCATION**: F1=0.100 - находит локации, но много FP

**Слабые стороны:**
- **PHONE**: F1=0.000 - не работает с телефонами
- **EMAIL**: F1=0.000 - не работает с email
- **Высокий FP**: много ложных срабатываний

**Технология:**
```python
NATASHA_ENTITIES = {
    'PER': 'PERSON',        # Отлично работает
    'LOC': 'LOCATION',      # Много ложных срабатываний  
    'ORG': 'ORGANIZATION',  # Лучший результат среди всех
}
```

### 3. 🌍 SpaCy Extended Anonymizer  

**Файл:** `anonymizers/spacy_extended/spacy_anonymizer.py`

**Описание:** Универсальный анонимизатор на основе SpaCy.

**Сильные стороны:**
- **PERSON**: F1=0.831 (отлично) - 76 TP, 18 FP, 13 FN
- **LOCATION**: F1=0.115 - полный recall (7/7), но много FP
- **POSITION**: F1=0.182 - единственный, кто находит должности

**Слабые стороны:**
- **PHONE**: F1=0.000 - не работает с телефонами  
- **EMAIL**: F1=0.000 - не работает с email
- **AGE**: F1=0.205 - слабая работа с возрастом

### 4. 🔄 Hybrid Advanced Anonymizer

**Файл:** `anonymizers/hybrid_advanced/hybrid_anonymizer.py`

**Описание:** Комбинирует несколько подходов, но результаты не оправдывают ожидания.

**Результаты:** F1 Micro=0.236, F1 Macro=0.066 (ниже ожидаемого)
- Должен был быть лучшим, но показывает средние результаты
- Проблемы с интеграцией разных методов
- Требует доработки алгоритма объединения
- **Крайне низкий F1 Macro** - плохо работает с редкими типами

### 5. 🧠 DeepPavlov NER Anonymizer

**Файл:** `anonymizers/deeppavlov_ner/deeppavlov_anonymizer.py`

**Описание:** Использует BERT для NER, но требует дообучения.

**Текущие проблемы:**
- **Общие метрики**: F1 Micro=0.205, F1 Macro=0.056 - низкие результаты для BERT
- **PERSON**: F1=0.804 - единственная сильная сторона
- **Остальные типы**: практически не работают
- **Скорость**: очень медленная (~2000 мс на 1000 символов)
- **F1 Macro**: один из худших показателей (0.056)

**Требует:** Дообучение на русскоязычных данных персональных данных.

### 6. 🔬 Transformers BERT Anonymizer

**Файл:** `anonymizers/transformers_bert/transformers_anonymizer.py`

**Описание:** Экспериментальный анонимизатор, находится в разработке.

**Текущее состояние:**
- **Общие метрики**: F1 Micro=0.190, F1 Macro=0.042 - самые низкие результаты
- **PERSON**: F1=0.764 - хорошо находит имена
- **ADDRESS**: много ложных срабатываний
- **Статус**: требует значительной доработки
- **Критически низкий F1 Macro**: 0.042 - худший среди всех методов

## 🎯 Практические рекомендации

### По типам задач:

**Для обработки телефонов и email:**
```python
# Используйте только RegExp Baseline
from anonymizers.regexp_baseline.regexp_anonymizer import RegExpBaselineAnonymizer
anonymizer = RegExpBaselineAnonymizer()
```

**Для обработки имен людей:**
```python
# Лучший выбор: Natasha или SpaCy
from anonymizers.natasha_enhanced.natasha_anonymizer import NatashaAnonymizer
anonymizer = NatashaAnonymizer(aggressiveness=0.7)
```

**Для комплексной обработки:**
```python
# Создайте собственный комбинированный подход:
def combined_anonymize(text):
    # 1. RegExp для телефонов/email
    regexp_result = regexp_anonymizer.anonymize(text)
    
    # 2. Natasha для имен
    natasha_result = natasha_anonymizer.anonymize(regexp_result)
    
    return natasha_result
```

## 📊 Детальная статистика по типам

### Лучшие анонимизаторы по типам сущностей:

| Тип сущности | Лучший анонимизатор | F1-Score | Примечания |
|--------------|-------------------|----------|------------|
| **PHONE** | RegExp Baseline | **0.937** | 🏆 Единственный работающий |
| **EMAIL** | RegExp Baseline | **0.556** | 🏆 Единственный работающий |
| **PERSON** | Natasha Enhanced | **0.862** | ✅ Отлично для русских имен |
| **ORGANIZATION** | Natasha Enhanced | **0.291** | ⚠️ Много FP, но лучший |
| **LOCATION** | SpaCy Extended | **0.115** | ⚠️ Полный recall, но много FP |
| **AGE** | Hybrid Advanced | **0.211** | ❌ Все работают плохо |
| **BANK_CARD** | RegExp Baseline | **0.913** | 🏆 Отличный результат |

### Проблемные типы (требуют доработки):
- **SNILS**: максимум F1=0.240 (RegExp)
- **VIN_NUMBER**: F1=0.000 у всех
- **PASSPORT**: максимум F1=0.278 (RegExp)
- **DRIVER_LICENSE**: F1=0.000 у всех
- **IP_ADDRESS**: F1=1.000 только у RegExp

## 🔧 Рекомендации по улучшению

### Приоритет 1: Создание комбинированного анонимизатора
```python
class OptimalAnonymizer:
    def __init__(self):
        self.regexp = RegExpBaselineAnonymizer()  # для PHONE, EMAIL, BANK_CARD
        self.natasha = NatashaAnonymizer()        # для PERSON, ORGANIZATION
        self.spacy = SpacyAnonymizer()           # для LOCATION
    
    def anonymize(self, text):
        # Применяем методы последовательно
        result = self.regexp.anonymize(text)
        result = self.natasha.anonymize(result)
        result = self.spacy.anonymize(result)
        return result
```

### Приоритет 2: Улучшение существующих методов
- **RegExp**: добавить паттерны для PERSON, ADDRESS
- **Natasha**: снизить количество ложных срабатываний
- **SpaCy**: добавить регулярные выражения для PHONE/EMAIL
- **Hybrid**: переработать алгоритм объединения результатов

### Приоритет 3: Новые подходы
- Обучение специализированных моделей на русских ПД
- Использование LLM для контекстуальной анонимизации
- Применение активного обучения для улучшения результатов

## 📈 Целевые метрики

Для использования в продакшене рекомендуются следующие целевые показатели:

| Метрика | Минимум | Целевое значение | Текущий лучший |
|---------|---------|------------------|----------------|
| **F1 Micro общий** | 0.80 | 0.90+ | 0.499 (RegExp) |
| **F1 Macro общий** | 0.60 | 0.80+ | 0.275 (RegExp) |
| **Precision** | 0.85 | 0.95+ | 0.654 (RegExp) |
| **Recall** | 0.75 | 0.85+ | 0.403 (RegExp) |
| **Скорость** | <100 мс | <50 мс | ~10 мс (RegExp) |

### 🎯 Критические выводы по F1 Macro

**Текущее состояние:**
- Лучший F1 Macro: 0.275 (RegExp Baseline)
- Остальные методы: 0.042-0.074 (критически низко)
- **Проблема**: все методы плохо работают с редкими типами сущностей

**Рекомендации:**
1. **Повысить F1 Macro до 0.6+** - ключевой приоритет развития
2. **Сбалансировать работу** со всеми типами сущностей
3. **Избегать переоптимизации** только под частые типы (PERSON, PHONE)
