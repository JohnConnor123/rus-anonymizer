# Модули генерации датасета

Этот пакет содержит модули для генерации датасета анонимизации персональных данных через OpenAI API.

## Модули

- **`dataset_config.py`** - Конфигурации, случайное семплирование параметров
- **`dataset_prompts.py`** - Шаблоны промптов для разных сфер и регионов
- **`dataset_openai_generator.py`** - Основной класс генератора через OpenAI API

## Использование

Основной скрипт находится в корне проекта:

```bash
python generate_dataset.py --help
```

Для тестирования модулей:

```bash
cd tests
python test_dataset_generator.py 
```

# Скрипты для работы с датасетами

Данная папка содержит скрипты для обработки и разметки датасетов персональных данных.

## Скрипты

### 1. `split_dataset.py` - Разбиение датасета

Разбивает большой датасет на поддатасеты по заданному количеству диалогов.

**Использование:**
```bash
python scripts/split_dataset.py data/generated/dataset.json --dialogs_per_subset 50
```

**Параметры:**
- `input_file` - путь к исходному файлу датасета
- `--dialogs_per_subset` / `-d` - количество диалогов в каждом поддатасете (по умолчанию: 50)

**Результат:**
- Создает файлы `dataset.1.json`, `dataset.2.json`, и т.д.
- Сохраняет метаданные о родительском датасете

### 2. `annotate_dataset.py` - Супер-умная разметка

Высококачественный скрипт для автоматической разметки персональных данных с умной фильтрацией ложных срабатываний.

**Использование:**
```bash
python scripts/annotate_dataset.py
```

**Возможности:**
- 🎯 **Высокая точность** - умная валидация найденных сущностей
- 🚫 **Фильтрация ложных срабатываний** - исключает номера заказов, обычные фразы
- 📊 **Подробная статистика** - показывает распределение по типам сущностей
- 🔍 **Расширенное покрытие** - поддерживает все основные типы ПД
- ⚡ **Автоматический поиск** - находит все поддатасеты в папке
- 💾 **Безопасность** - создает новые файлы, не изменяя исходные

## Поддерживаемые типы ПД

| Тип | Описание | Примеры |
|-----|----------|---------|
| PHONE | Телефонные номера | +7 916 123 45 67, 8 903 123 45 67 |
| EMAIL | Электронная почта | user@example.com |
| PASSPORT | Паспортные данные | серия 1234 номер 567890 |
| DRIVER_LICENSE | Водительские права | 12АВ123456 |
| CARD_NUMBER | Номера банковских карт | 1234 5678 9012 3456 |
| SNILS | СНИЛС | 123-456-789 12 |
| INN | ИНН | 1234567890 |
| VIN | VIN номер автомобиля | XW8ZZZ4C9DW123456 |
| ADDRESS | Адреса | г. Москва, ул. Тверская, д. 1 |
| FULL_NAME | Полные имена | (только с контекстом) |

## Умная валидация

Скрипт применяет интеллектуальную фильтрацию:

- ✅ **Телефоны**: проверяет длину (10-11 цифр), исключает номера заказов
- ✅ **Email**: валидирует формат (наличие @ и домена)
- ✅ **Банковские карты**: проверяет 16 цифр, исключает номера заказов
- ✅ **СНИЛС**: строгая проверка формата XXX-XXX-XXX XX
- ✅ **VIN**: проверяет ровно 17 символов
- ✅ **Имена**: исключает обычные фразы ("добрый день", "все верно")

## Структура выходных файлов

Размеченные файлы содержат:

```json
{
  "metadata": {
    "total_dialogs": 50,
    "annotation_version": "smart_v3.0",
    "annotation_stats": {
      "dialogs_with_pd": 15,
      "pd_percentage": 30.0,
      "total_entities_found": 25,
      "entity_types_distribution": {
        "PHONE": 8,
        "EMAIL": 5,
        "PASSPORT": 2
      }
    }
  },
  "dialogs": [
    {
      "text": "Мой телефон +7 916 123 45 67",
      "entities": [
        {
          "start": 12,
          "end": 28,
          "type": "PHONE",
          "text": "+7 916 123 45 67"
        }
      ],
      "has_pd": true,
      "description": "...",
      "id": 1
    }
  ]
}
```

## Быстрый старт

1. **Разбейте большой датасет** (если нужно):
   ```bash
   python scripts/split_dataset.py data/generated/big_dataset.json -d 50
   ```

2. **Выполните разметку**:
   ```bash
   python scripts/annotate_dataset.py
   ```

3. **Проверьте результаты** в файлах `*_annotated.json`

## Особенности

- 🔄 **Автоматический поиск** - находит все файлы `llama-4-maverick-4.*.json`
- 🛡️ **Безопасность** - исключает уже размеченные файлы
- 📈 **Статистика** - подробные метрики по каждому поддатасету
- 🎨 **Удобный вывод** - эмодзи и цветная индикация прогресса

# Скрипты проекта Anonymizer

Этот пакет содержит основные исполняемые скрипты для работы с системой анонимизации персональных данных.

## 📋 Основные скрипты

### 🔬 Валидация и тестирование

#### `validate_simple.py` - Валидация всех анонимизаторов
Основной скрипт для быстрой валидации и сравнения производительности анонимизаторов.

```bash
# Валидация с автопоиском датасета
python scripts/validate_simple.py

# Валидация конкретного датасета
python scripts/validate_simple.py --dataset data/processed/merged_dataset.json
```

**Возможности:**
- ✅ Тестирование всех доступных анонимизаторов
- ✅ Детальные метрики по типам сущностей  
- ✅ Экспорт результатов в CSV (data/reports/)
- ✅ Индикатор прогресса обработки
- ✅ Умное сопоставление сущностей

#### `validate_deeppavlov.py` - Специализированная валидация DeepPavlov
Углубленная валидация DeepPavlov NER с улучшенными алгоритмами.

```bash
# Требует отдельного окружения
source venv-deeppavlov/bin/activate
python scripts/validate_deeppavlov.py --dataset data/processed/merged_dataset.json --max-examples 50
```

### 🎲 Генерация данных

#### `generate_dataset.py` - Генерация синтетических датасетов
Создание синтетических диалогов с персональными данными через OpenAI API.

```bash
# Настройте config.env и запустите
source venv/bin/activate
python scripts/generate_dataset.py
```

**Настройка через config.env:**
- `OPENAI_API_KEY` - API ключ OpenAI
- `TOTAL_DIALOGS=100` - количество диалогов
- `MODEL=gpt-4o-mini` - модель для генерации

### ⚙️ Вспомогательные модули

#### `dataset_config.py` - Конфигурации генерации
Настройки для различных типов диалогов и сфер деятельности.

#### `dataset_openai_generator.py` - Генератор через OpenAI API
Основной класс для взаимодействия с OpenAI API и генерации данных.

#### `dataset_prompts.py` - Шаблоны промптов
Промпты для генерации диалогов в разных сферах (медицина, банки, образование).

#### `config_loader.py` - Загрузчик конфигурации
Модуль для загрузки настроек из файла config.env.

## 🔄 Рекомендуемый workflow

### 1. Генерация тестовых данных
```bash
# Настройте config.env
cp config.env.example config.env
nano config.env

# Генерируйте данные
python scripts/generate_dataset.py
```

### 2. Валидация анонимизаторов  
```bash
# Основная валидация
python scripts/validate_simple.py --dataset data/generated/your_dataset.json

# Результаты сохраняются в data/reports/
ls data/reports/
```

### 3. Анализ результатов
```bash
# Просмотр CSV отчетов
python -c "
import pandas as pd
df = pd.read_csv('data/reports/your_dataset_summary_all_methods.csv', sep=';')
print(df[df['entity_type'] == 'ALL'].sort_values('f1', ascending=False))
"
```

## 📊 Поддерживаемые типы сущностей

Все скрипты поддерживают следующие типы персональных данных:

| Тип | Описание | Примеры |
|-----|----------|---------|
| **PERSON** | Полные имена | Иван Петров, И.И. Сидоров |
| **PHONE** | Телефонные номера | +7 916 123 45 67, 8(495)123-45-67 |
| **EMAIL** | Электронная почта | user@example.com |
| **ADDRESS** | Адреса | г. Москва, ул. Тверская, д. 1 |
| **ORGANIZATION** | Организации | ООО "Рога и Копыта", Сбербанк |
| **PASSPORT** | Паспортные данные | серия 1234 номер 567890 |
| **SNILS** | СНИЛС | 123-456-789 12 |
| **INN** | ИНН | 1234567890 |
| **BANK_CARD** | Номера карт | 1234 5678 9012 3456 |
| **DRIVER_LICENSE** | Водительские права | 12АВ123456 |
| **VIN_NUMBER** | VIN автомобиля | XW8ZZZ4C9DW123456 |
| **CAR_NUMBER** | Автомобильные номера | А123БВ77 |
| **AGE** | Возраст | 25 лет, мне 30 |
| **BIRTHDAY** | Дни рождения | 01.01.1990 |
| **POSITION** | Должности | директор, главный бухгалтер |
| **EDUCATION** | Образование | МГУ, высшее образование |
| **MEDICAL** | Медицинские данные | группа крови, аллергии |
| **IP_ADDRESS** | IP адреса | 192.168.1.1 |
| **BANK_ACCOUNT** | Банковские счета | 40817810123456789012 |
| **LOCATION** | Географические места | Москва, Россия |
| **FAMILY** | Семейное положение | женат, разведен |

## 📦 Зависимости

### Основное окружение (venv)
```bash
pip install -r requirements.txt
```

### DeepPavlov окружение (venv-deeppavlov)  
```bash
pip install -r requirements-deeppavlov.txt
python -m deeppavlov install ner_rus_bert
```

## 💡 Полезные советы

- **Для больших датасетов** используйте `--max-examples` ограничение
- **CSV отчеты** автоматически сохраняются в `data/reports/`
- **Конфигурацию** всегда проверяйте через `config.env`
- **DeepPavlov** требует отдельного виртуального окружения
- **Результаты** включают детальную статистику по каждому типу сущности